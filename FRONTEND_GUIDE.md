# Xray 管理器 - 前端功能使用指南

## 🎨 界面布局

### 整体布局
```
┌──────────────────────────────────────────────────────┐
│  [侧边栏]           [主内容区]                        │
│  ┌────────┐         ┌──────────────────────────┐    │
│  │分组列表│         │   规则表格               │    │
│  │        │         │   (显示所有节点)          │    │
│  │        │         └──────────────────────────┘    │
│  │        │         ┌──────────────────────────┐    │
│  │订阅管理│         │   日志输出               │    │
│  └────────┘         │   (带搜索和过滤)          │    │
│                     └──────────────────────────┘    │
│                     [控制按钮区域]                    │
└──────────────────────────────────────────────────────┘
```

---

## 📋 新增功能详解

### 1. 侧边栏分组列表 ✅

**位置**: 窗口左侧

**功能**:
- 显示所有节点分组
- 点击分组可过滤显示该分组下的节点
- "所有节点"：显示全部节点
- 分组右上角显示"订阅"标签（来自订阅的分组）
- 手动创建的分组可以删除（订阅分组不可删除）

**操作**:
- 点击 `+` 按钮：创建新分组
- 点击分组名称：过滤显示该分组节点
- 点击 `×` 按钮：删除手动创建的分组

---

### 2. 订阅管理 ✅

**入口**: 侧边栏底部的"订阅管理"按钮

**功能**:
- 添加订阅：输入订阅名称、URL、自动更新设置
- 更新订阅：手动触发订阅更新
- 删除订阅：删除订阅及其所有节点
- 显示订阅信息：节点数、类型、更新时间等

**支持的订阅格式**:
- ✅ Clash YAML
- ✅ V2Ray Base64
- ✅ SIP008 Shadowsocks JSON
- ✅ vmess://, vless://, ss://, trojan:// 链接

**操作流程**:
1. 点击"订阅管理"按钮
2. 点击"添加订阅"
3. 填写订阅信息：
   - 订阅名称：如"我的订阅"
   - 订阅地址：订阅URL
   - 自动更新：勾选后自动定时更新
   - 更新间隔：默认6小时
4. 点击"添加"，等待导入完成
5. 订阅节点会自动创建对应分组

**自动更新特性**:
- 自动检测新增节点（自动添加）
- 自动检测删除节点（自动删除）
- 自动检测配置变化（自动更新）
- 保留节点的启用状态

---

### 3. 节点测速 ✅

**位置**: 规则表格中的新列

**显示内容**:
- **延迟列**: 显示TCP延迟（毫秒）
  - 绿色：< 100ms（优秀）
  - 黄色：100-300ms（一般）
  - 红色：> 300ms（较差）
- **速度列**: 显示下载速度（MB/s）

**操作方式**:
- **单个测速**: 点击规则行的 `🚀` 按钮
- **批量测速**: 点击底部的"批量测速"按钮

**测速说明**:
- 未启动的节点：只测试延迟
- 已启动的节点：测试延迟 + 下载速度
- 测速中显示"测试中..."
- 测速完成后自动更新表格
- 测速结果保存到配置文件

**测速原理**:
1. TCP延迟：直接连接代理服务器测试响应时间
2. 下载速度：通过本地代理下载1MB测试文件

---

### 4. 日志搜索和过滤 ✅

**位置**: 日志输出框上方

**功能控件**:
- **搜索框**: 输入关键字实时搜索日志
- **级别过滤**: 下拉选择日志级别
  - 所有日志
  - INFO
  - WARN
  - ERROR
  - XRAY（Xray进程输出）
- **清空日志**: 清空所有日志

**使用方法**:
1. 在搜索框输入关键字（如"失败"、"成功"）
2. 或选择日志级别过滤
3. 可以同时使用搜索和级别过滤
4. 日志实时更新，自动应用过滤

**日志特性**:
- 保存最近1000条日志
- 支持中文和英文搜索
- 不区分大小写
- 搜索日志消息和来源

---

### 5. 端口检测 ✅

**位置**: 添加/编辑规则对话框中的"本地代理端口"字段

**功能**:
- 实时检测端口是否可用
- 显示端口状态：
  - `✓ 可用`（绿色）
  - `× 已占用`（红色）
- 推荐可用端口按钮

**使用方法**:
1. 在端口输入框输入端口号
2. 失去焦点后自动检测可用性
3. 或点击"推荐端口"按钮获取可用端口

**推荐端口逻辑**:
- 优先推荐常用端口：10808, 10809, 10810, 1080, 1081 等
- 如果都被占用，从10800开始查找
- 自动跳过已占用端口

---

### 6. 分组标签 ✅

**位置**: 规则表格的"别名"列下方

**显示**:
- 小灰色标签显示节点所属分组
- 只显示有分组的节点
- 帮助快速识别节点归属

**示例**:
```
[节点别名]
美国节点组
```

---

## 🎯 完整操作流程示例

### 场景1：添加订阅并测速

1. 点击侧边栏底部的"订阅管理"
2. 点击"添加订阅"
3. 填写：
   - 名称：我的订阅
   - 地址：https://example.com/sub
   - 勾选"自动更新"
   - 间隔：6小时
4. 点击"添加"，等待节点导入
5. 返回主界面，在侧边栏看到新分组
6. 点击分组名称，查看该分组节点
7. 点击"批量测速"按钮
8. 等待测速完成，查看延迟和速度结果
9. 根据测速结果选择优质节点启动

### 场景2：手动创建分组管理节点

1. 点击侧边栏顶部的 `+` 按钮
2. 填写分组名称和描述
3. 点击"添加"
4. 手动添加节点时，可以选择该分组（待实现）
5. 点击分组查看该分组下的所有节点

### 场景3：搜索和过滤日志

1. 在日志搜索框输入"错误"
2. 选择日志级别"ERROR"
3. 查看所有包含"错误"的ERROR级别日志
4. 点击"清空日志"清除所有日志

### 场景4：端口冲突处理

1. 点击"添加规则"
2. 输入本地端口10808
3. 看到"× 已占用"提示
4. 点击"推荐端口"按钮
5. 自动填入可用端口10809
6. 看到"✓ 可用"提示
7. 继续配置其他选项

---

## 🎨 UI 改进

### 颜色系统
- **侧边栏**: 深色主题 (#2c3e50)
- **测速结果**:
  - 绿色：优秀
  - 黄色：一般
  - 红色：较差
- **分组标签**: 订阅分组显示红色标签
- **状态提示**:
  - 绿色：可用/成功
  - 红色：占用/失败

### 响应式设计
- 侧边栏固定220px宽度
- 主内容区自适应
- 表格列宽优化，适应新增列
- 滚动条仅在需要时显示

---

## 💡 使用技巧

### 1. 快速筛选节点
- 使用侧边栏分组快速切换显示
- 使用日志搜索快速定位问题

### 2. 优化节点选择
- 使用批量测速功能测试所有节点
- 根据延迟和速度选择最优节点
- 延迟<100ms 且速度>10MB/s 的节点为最佳

### 3. 订阅管理最佳实践
- 设置自动更新间隔为6-12小时
- 定期手动更新重要订阅
- 删除不再使用的订阅节省资源

### 4. 日志分析
- 使用ERROR级别快速定位错误
- 搜索节点名称查看特定节点日志
- 定期清空日志保持界面清爽

---

## ⚠️ 注意事项

1. **测速注意事项**:
   - 批量测速可能需要较长时间
   - 测速会消耗流量
   - 建议在网络空闲时进行

2. **订阅更新**:
   - 自动更新会静默执行
   - 更新时不会中断正在运行的节点
   - 删除节点时会自动停止

3. **端口管理**:
   - 导入订阅时自动分配端口
   - 手动添加时建议使用推荐端口
   - 避免使用系统保留端口（<1024）

4. **分组操作**:
   - 订阅分组不可删除
   - 手动分组删除前需确保无节点
   - 删除分组不会删除节点

---

## 🔧 故障排除

### 问题1：订阅导入失败
**可能原因**:
- 订阅链接无效或过期
- 网络连接问题
- 订阅格式不支持

**解决方法**:
- 检查订阅链接是否正确
- 尝试手动访问订阅链接
- 查看日志中的详细错误信息

### 问题2：测速一直显示"测试中"
**可能原因**:
- 节点无法连接
- 网络超时
- 防火墙阻止

**解决方法**:
- 检查节点配置是否正确
- 尝试重启节点
- 查看日志中的错误信息

### 问题3：日志搜索无结果
**可能原因**:
- 关键字拼写错误
- 级别过滤不匹配
- 日志已清空

**解决方法**:
- 检查关键字拼写
- 选择"所有日志"级别
- 重新触发操作生成日志

---

## ✅ 新增完善功能

### 1. 节点编辑时选择分组 ✅

**位置**: 添加/编辑规则对话框

**功能**:
- 在规则对话框中添加"所属分组"选择器
- 支持选择手动创建的分组（不包括订阅分组）
- 后端自动根据 GroupID 设置 GroupName
- 保留订阅节点的来源信息

**使用方法**:
1. 打开添加或编辑规则对话框
2. 在"基本信息"区域选择"所属分组"
3. 从下拉列表中选择分组或选择"无分组"
4. 保存规则后，节点会自动关联到选中的分组

---

### 2. 测速结果排序 ✅

**位置**: 规则表格的延迟和速度列标题

**功能**:
- 点击"延迟"列标题按延迟排序
- 点击"速度"列标题按速度排序
- 支持升序/降序切换
- 显示排序指示器（▲升序，▼降序）

**使用方法**:
1. 点击"延迟"或"速度"列标题
2. 第一次点击：升序排序
3. 再次点击：降序排序
4. 排序状态会保持，直到刷新或选择其他排序列

---

### 3. 导出测速报告 ✅

**位置**: 底部控制面板的"导出测速报告"按钮

**功能**:
- 导出所有已测速节点的报告
- CSV 格式，支持中文
- 包含完整信息：别名、分组、协议、地址、端口、延迟、速度、测试时间、状态
- 文件名包含时间戳

**使用方法**:
1. 先进行节点测速（单个或批量）
2. 点击"导出测速报告"按钮
3. 浏览器自动下载 CSV 文件
4. 可用 Excel 或其他表格软件打开

**报告内容**:
- 延迟 < 100ms：优秀
- 延迟 100-300ms：一般
- 延迟 > 300ms：较差

---

### 4. 分组批量操作（启动/停止）✅

**位置**: 侧边栏分组列表项

**功能**:
- 每个分组右侧显示启动和停止按钮
- 一键启动分组内所有节点
- 一键停止分组内所有节点
- 支持订阅分组和手动分组

**使用方法**:
1. 在侧边栏找到目标分组
2. 点击 ▶ 按钮启动分组所有节点
3. 点击 ■ 按钮停止分组所有节点
4. 操作前会弹出确认对话框
5. 日志中显示操作进度和结果

---

### 5. 节点搜索功能 ✅

**位置**: 底部控制面板右侧的搜索框

**功能**:
- 实时搜索节点
- 支持搜索：别名、服务器地址、协议、分组名称、本地端口
- 不区分大小写
- 搜索结果与排序兼容

**使用方法**:
1. 在搜索框输入关键字
2. 表格自动过滤显示匹配节点
3. 清空搜索框恢复全部显示
4. 搜索结果可继续排序

**搜索范围**:
- 节点别名
- 服务器地址
- 协议类型
- 分组名称
- 本地端口

---

### 6. 批量启动性能优化 ✅

**优化内容**:
- 分批并行处理（每批10个节点）
- 批次间延迟500ms防止资源耗尽
- 显示详细进度信息
- 统计成功/失败数量

**性能提升**:
- 原方案：串行处理，100个节点约需 100-200秒
- 新方案：批量并行，100个节点约需 50-60秒
- 提升约 50% 的处理速度
- UI 更流畅，不会卡顿

**适用场景**:
- 启动/停止大量选中节点
- 分组批量操作
- 订阅更新后批量启动

---

## 📝 快捷键（待实现）

计划支持的快捷键：
- `Ctrl/Cmd + F`: 聚焦节点搜索
- `Ctrl/Cmd + N`: 添加新规则
- `Ctrl/Cmd + S`: 保存规则
- `Esc`: 关闭对话框

---

## 🚀 后续改进计划

- [x] 节点编辑时选择分组
- [x] 测速结果排序（按延迟/速度）
- [x] 导出测速报告
- [x] 分组批量操作（启动/停止）
- [x] 节点搜索功能
- [x] 优化批量启动性能
- [ ] 测速进度条显示
- [ ] 节点收藏功能
- [ ] 自定义测速服务器
- [ ] 深色主题切换
- [ ] 历史测速记录

---

## 📚 相关文档

- [后端功能文档](FEATURES.md)
- [API 使用文档](FEATURES.md#使用示例)
- [开发指南](README.md)

---

**享受全新的 Xray 管理器体验！** 🎉
